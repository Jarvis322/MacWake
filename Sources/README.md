# App Sources

`Sources/` contains the macOS menu-bar application target. `ChargeLimitManager.swift` owns the privileged-helper-backed charge-limit and calibration control loop; `BatteryTracker.swift` supplies battery and power-source updates.
