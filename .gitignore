.DS_Store
.build/
*.dSYM/
MacWake.app/
AppIcon.iconset/
.tempmediaStorage/
*~
*.swp

# Xcode / xcodegen — project is generated from project.yml via `xcodegen generate`
MacWake.xcodeproj/
Generated/
.xcbuild/
DerivedData/
*.xcuserstate

# Distribution artifacts
*.dmg
*.zip
AppIcon.icns

# Dev / debug scratch files
*.log
*.txt
test_*.swift
dump_*.swift
scan_*.swift
list_smc.swift
appstore-build/
.build-appstore/

# Signing certs — NEVER commit private keys
*.p12
MacWake-Secrets/

# App Store build tooling — local only, keep out of the public repo
AppStore/
